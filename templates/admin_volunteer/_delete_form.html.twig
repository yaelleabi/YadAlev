<form method="post" action="{{ path('app_admin_volunteer_delete', {'id': volunteer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ volunteer.id) }}">
    <button class="btn">Delete</button>
</form>
