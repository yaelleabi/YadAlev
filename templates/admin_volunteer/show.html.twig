{% extends 'base.html.twig' %}

{% block title %}Détails bénévole - Admin{% endblock %}

{% block body %}

<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-2 fixed-top">
    <div class="container d-flex justify-content-between align-items-center">

        <div class="d-flex align-items-center gap-3">
            <img src="{{ asset('images/LOGO.PNG') }}" alt="Logo Yad Alev" class="nav-logo">
            <span class="fw-bold text-primary fs-5">Détails du bénévole</span>
        </div>

        <div class="d-flex align-items-center gap-4">
            <a href="{{ path('app_admin_volunteer_index') }}" class="nav-icon" title="Retour liste">
                <i class="fas fa-list"></i>
            </a>
            <a href="{{ path('app_logout') }}" class="nav-icon" title="Se déconnecter">
                <i class="fas fa-sign-out-alt"></i>
            </a>
        </div>

    </div>
</nav>

<!-- PAGE -->
<div class="main-container">
    <div class="content-wrapper container" style="padding-top: 100px;">

        <div class="text-center mb-5">
            <h2 class="text-primary fw-bold fade-in">👤 Détails du bénévole</h2>
            <p class="text-muted fade-in" style="animation-delay:.2s">
                Informations du compte et événements associés.
            </p>
        </div>

        <div class="info-card shadow-sm fade-in p-4">

            <div class="d-flex justify-content-between align-items-start flex-wrap gap-3 mb-3">
                <div>
                    <h4 class="mb-1">{{ volunteer.name }}</h4>
                    <h4 class="mb-1">{{ volunteer.firstName }}</h4>
                </div>

               
            </div>

            <hr class="my-4">

            <h5 class="text-primary fw-semibold mb-3">Informations</h5>

            <div class="table-responsive">
                <table class="table align-middle">
                    <tbody>
                        
                        <tr>
                            <th>Email</th>
                            <td>{{ volunteer.email }}</td>
                        </tr>   
                        <tr>
                            <th>Téléphone</th>
                            <td>{{ volunteer.phoneNumber }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            <hr class="my-4">

            <h5 class="text-primary fw-semibold mb-3">Événements du bénévole</h5>

            {% if volunteer.volunteerEvents is empty %}
                <div class="text-muted">
                    <i class="fas fa-calendar-times me-1"></i> Aucun événement associé.
                </div>
            {% else %}
                <ul class="list-group">
                    {% for event in volunteer.volunteerEvents %}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <div>
                                {# Adapte ces champs selon ton VolunteerEvent #}
                                <strong>{{ event.title ?? ('Événement #' ~ event.id) }}</strong>
                            
                            </div>

                            {# Optionnel: lien vers la page show event si tu en as une #}
                            {# <a class="btn btn-sm btn-outline-primary" href="{{ path('app_admin_volunteer_event_show', {'id': event.id}) }}">
                                <i class="fas fa-eye"></i>
                            </a> #}
                        </li>
                    {% endfor %}
                </ul>
            {% endif %}

        </div>

        <div class="text-center mt-4 fade-in" style="animation-delay:.25s">
            <a href="{{ path('app_admin_volunteer_index') }}" class="btn btn-outline-secondary btn-lg shadow-sm">
                ← Retour à la liste des bénévoles
            </a>
        </div>

        <footer class="footer-custom text-center text-muted small py-4 mt-5">
            <div class="footer-line mx-auto"></div>
            <p class="m-0">© 2025 Yad Alev — Tous droits réservés.</p>
        </footer>

    </div>
</div>

<style>
.navbar { border-bottom: 1px solid #e2e8f0; }
.nav-logo { width: 48px; }

.nav-icon { font-size: 1.4rem; color:#444; transition:.2s; }
.nav-icon:hover { color:#0d6efd; transform: translateY(-2px); }

.main-container { min-height: 100vh; display:flex; flex-direction:column; }

.info-card { background:#fff; border-radius:1rem; border:1px solid #e2e8f0; }

.fade-in { opacity:0; animation: fadeSlide .7s ease forwards; }
@keyframes fadeSlide { from{opacity:0; transform:translateY(25px);} to{opacity:1; transform:none;} }

.footer-line { width:85%; height:1px; background:#d0d7e0; }
</style>

{% endblock %}
