{% extends 'base.html.twig' %}

{% block title %}Liste des bénévoles - Admin{% endblock %}

{% block body %}

<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-2 fixed-top">
    <div class="container d-flex justify-content-between align-items-center">

        <div class="d-flex align-items-center gap-3">
            <a href="{{ path('app_admin') }}">
                <img src="{{ asset('images/LOGO.PNG') }}" alt="Logo Yad Alev" class="nav-logo">
            </a>
            <span class="fw-bold text-primary fs-5">Gestion des bénévoles</span>
        </div>

        <div class="d-flex align-items-center gap-4">
            <a href="{{ path('app_admin') }}" class="nav-icon" title="Accueil admin">
                <i class="fas fa-home"></i>
            </a>
            <a href="{{ path('app_logout') }}" class="nav-icon" title="Se déconnecter">
                <i class="fas fa-sign-out-alt"></i>
            </a>
        </div>

    </div>
</nav>

<div class="main-container">

    <div class="content-wrapper container" style="padding-top: 100px;">

        <div class="text-center mb-5">
            <h2 class="text-primary fw-bold fade-in">🤝 Liste des bénévoles</h2>
            <p class="text-muted fade-in" style="animation-delay:.2s">
                Consultez et gérez les comptes bénévoles.
            </p>
        </div>

        <div class="d-flex justify-content-end mb-3 fade-in" style="animation-delay:.25s">
            <a href="{{ path('app_admin_volunteer_new') }}" class="btn btn-primary btn-lg shadow-sm">
                ➕ Créer un bénévole
            </a>
        </div>

        <div class="info-card shadow-sm fade-in">
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Nom</th>
                            <th>Prénom</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>

                    <tbody>
                    {% for volunteer in volunteers %}
                        <tr>
                    
                            <td>{{ volunteer.name }}</td>
                            <td>{{ volunteer.firstName }}</td>

                            <td class="text-center">
                                <div class="d-flex justify-content-center align-items-center gap-1">
                                    
                                    <a href="{{ path('app_admin_volunteer_show', {'id': volunteer.id}) }}"
                                       class="btn btn-sm btn-outline-primary" title="Voir">
                                        <i class="fas fa-eye"></i>
                                    </a>

                                    <a href="{{ path('app_admin_volunteer_edit', {'id': volunteer.id}) }}"
                                       class="btn btn-sm btn-outline-success" title="Modifier">
                                        <i class="fas fa-edit"></i>
                                    </a>

                                    <form method="post" 
                                          action="{{ path('app_admin_volunteer_delete', {'id': volunteer.id}) }}" 
                                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce bénévole ?');"
                                          class="d-inline-block m-0 p-0">
                                        
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ volunteer.id) }}">
                                        
                                        <button type="submit" class="btn btn-sm btn-outline-danger" title="Supprimer">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>

                                </div>
                            </td>
                        </tr>

                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center py-4 text-muted">
                                <i class="fas fa-user-slash"></i> Aucun bénévole enregistré.
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>

                </table>
            </div>
        </div>

        <footer class="footer-custom text-center text-muted small py-4 mt-5">
            <div class="footer-line mx-auto"></div>
            <p class="m-0">© 2025 Yad Alev — Tous droits réservés.</p>
        </footer>

    </div>
</div>

<style>
.navbar { border-bottom: 1px solid #e2e8f0; }
.nav-logo { width: 48px; }

.nav-icon {
    font-size: 1.4rem;
    color:#444;
    transition:.2s;
}
.nav-icon:hover {
    color:#0d6efd;
    transform: translateY(-2px);
}

.main-container {
    min-height: 100vh;
    display:flex;
    flex-direction:column;
}

.info-card {
    background:#fff;
    border-radius:1rem;
    padding:20px;
    border:1px solid #e2e8f0;
}

.table-hover tbody tr:hover {
    background:#eaf1ff;
    transition:.2s;
}

.fade-in {
    opacity:0;
    animation: fadeSlide .7s ease forwards;
}

@keyframes fadeSlide {
    from { opacity:0; transform:translateY(25px); }
    to { opacity:1; transform:none; }
}

.footer-line {
    width:85%;
    height:1px;
    background:#d0d7e0;
}
</style>

{% endblock %}