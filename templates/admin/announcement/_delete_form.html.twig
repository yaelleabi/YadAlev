<form method="post" action="{{ path('app_admin_announcement_delete', {'id': announcement.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette annonce ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ announcement.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
