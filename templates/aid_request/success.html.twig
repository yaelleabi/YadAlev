{% extends 'base.html.twig' %}

{% block title %}Demande envoyée avec succès{% endblock %}

{% block body %}
<div class="d-flex flex-column justify-content-center align-items-center min-vh-100 bg-light">
    <div class="card shadow-sm p-5 text-center" style="max-width: 600px; border-radius: 1rem;">
        <img src="{{ asset('images/LOGO.PNG') }}" alt="Logo Yad Alev" class="mb-4" style="width:120px;">

        <i class="bi bi-check-circle-fill text-success mb-3" style="font-size: 4rem;"></i>

        <h2 class="fw-bold text-success mb-3">Votre demande a bien été soumise</h2>
        <p class="text-muted fs-5">
            Merci d’avoir pris le temps de compléter votre demande.<br>
            Notre équipe l’examinera avec attention et reviendra vers vous prochainement.
        </p>

        <p class="text-secondary mt-4">
            <i class="bi bi-arrow-clockwise me-1"></i>
            Redirection automatique dans <span id="countdown" class="fw-semibold text-primary">5</span> secondes...
        </p>

        <a href="{{ path('app_family_home') }}" class="btn btn_success mt-3 px-4 rounded-pill">
            <i class="bi bi-house-door me-1"></i>Retourner à votre espace
        </a>
    </div>
</div>

<style>
body {
  background: linear-gradient(135deg, #f8f9fc, #eef2f7);
  overflow-x: hidden;
}
.card {
  border: 1px solid #e2e8f0;
  border-radius: 1rem;
  box-shadow: 0 10px 35px rgba(0, 0, 0, 0.1);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}
.card:hover {
  transform: translateY(-3px);
  box-shadow: 0 16px 50px rgba(0, 0, 0, 0.15);
}
</style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script>
        function initCountdown() {
            const countdownEl = document.getElementById('countdown');
            if (!countdownEl) return;

            let countdown = parseInt(countdownEl.textContent || '5', 10);
            const redirectUrl = "{{ path('app_family_home') }}";

            const timer = setInterval(() => {
                countdown--;
                countdownEl.textContent = countdown;
                if (countdown <= 0) {
                    clearInterval(timer);
                    window.location.href = redirectUrl;
                }
            }, 1000);
        }

        // Cas normal (rechargement direct)
        document.addEventListener("DOMContentLoaded", initCountdown);

        // Cas navigation Turbo (après redirection, sans reload complet)
        document.addEventListener("turbo:load", initCountdown);
    </script>
{% endblock %}
