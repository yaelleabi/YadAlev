{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-12 col-md-9 col-lg-7">

            <div class="card shadow-sm border-0">
                <div class="card-body p-4 p-md-5">

                    <h1 class="h4 mb-3">Vérifie ta boîte mail</h1>

                    <div class="alert alert-success mb-4" role="alert">
                        <strong>Si un compte correspond à cette adresse email</strong>,
                        un message vient d’être envoyé avec un lien de réinitialisation.
                    </div>

                    <p class="mb-2">
                        Le lien expirera dans
                        <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
                    </p>

                    <p class="text-muted mb-4">
                        Pense aussi à vérifier tes courriers indésirables (spam) et les onglets “Promotions”.
                    </p>

                    <div class="d-flex flex-column flex-sm-row gap-2">
                        <a class="btn btn-primary" href="{{ path('app_forgot_password_request') }}">
                            Renvoyer un email
                        </a>

                        {# Optionnel : si tu as une route de login #}
                        <a class="btn btn-outline-secondary" href="{{ path('app_login') }}">
                            Retour à la connexion
                        </a>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>
{% endblock %}
